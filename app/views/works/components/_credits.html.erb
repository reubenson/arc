<div class="work-credits-container<%= ' small' if small %> work-section">
  <% if @work.credits %>
  <section class="work-credits">
    <h5 class="work-section-header">Credits</h5>
    <%= raw @work.credits %>
  </section>
  <% end %>
</div>
